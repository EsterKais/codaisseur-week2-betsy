<%= form_for [@product, @product.orders.new] do |f| %>
  <p><strong>Price:</strong> <span>€</span> <%= @product.price %></p>

  <!-- <div class="form-group">
    <label>Amount</label>
    <%# amount %>
  </div> -->

  <%= f.hidden_field :product_id, value: @product.id %>
  <%= f.hidden_field :price, value: @product.price %>

  <div class="actions">
    <%= f.submit "Buy the Damn Cow Now!", class: "btn btn-success" %>
  </div>
<% end %>
